{
  "version": 2,
  "public": "src/public",
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node",
      "config": {
        "typescript": {
          "tsconfigPath": "tsconfig.json"
        },
        "includeFiles": ["node_modules/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "src/public/$1"
    }
  ]
}
